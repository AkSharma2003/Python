# frozenset is an immutable of pythin set

fs=frozenset([1,2,3])
print(fs)

# it charectrisic like set but reead only oprtaion is aplicable
# uses for read only data

# 2D forzen set 

fs1=frozenset([1,2,frozenset([3,4])])
print(fs1)
